.loading-state {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 80rpx 40rpx;
}

.loading-spinner {
  width: 48rpx;
  height: 48rpx;
  border: 4rpx solid var(--border-light, #eeeeee);
  border-top-color: var(--primary, #ff6b35);
  border-radius: 50%;
  animation: loading-spin 0.8s linear infinite;
  margin-bottom: 20rpx;
}

@keyframes loading-spin {
  to {
    transform: rotate(360deg);
  }
}

.loading-text {
  font-size: 28rpx;
  color: var(--text-secondary, #666666);
}
